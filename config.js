/* ===========================================================================
 * GRAMVERTER configuration
 *
 * Paste your FREE CoinGecko "Demo" API key below to get a higher, more stable
 * rate limit (~30 calls/min, 10,000/month) instead of the throttled shared
 * public pool. Create one here:
 *
 *     https://www.coingecko.com/en/developers/dashboard
 *
 * NOTE: this file is shipped to the browser, so the key is publicly visible.
 * A Demo key is designed for this and is safe to expose. NEVER put a paid /
 * Pro key here.
 * ========================================================================= */
window.GRAMVERTER_CONFIG = {
  coingeckoDemoKey: "", // e.g. "CG-xxxxxxxxxxxxxxxxxxxxxxxx"
};
